package com.cobblemon.mod.species;

/**
 * Silhouette family for model accessories and UI sprite drawing.
 * Original categories inspired by creature-collecting design language, not any IP.
 */
public enum BodyShape {
    /** Compact four-legged critter (kits, pups, cubs). */
    QUAD,
    /** Upright biped with arms (fighters, mid/final stages). */
    BIPED,
    /** Winged flier. */
    AVIAN,
    /** Fin / water-adapted body. */
    AQUATIC,
    /** Long tail-heavy / serpentine lean. */
    SERPENT,
    /** Bug-like shell + wings. */
    INSECTOID,
    /** Floating / leg-less ethereal. */
    AMORPH,
    /** Broad armored plates (rock / steel). */
    ARMORED,
    /** Soft sparkly fairy proportions. */
    FEY
}
